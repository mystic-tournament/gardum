#  Gardum

The repository contains addons as submodules. Use the `--recursive` flag on clone **or** call `git submodule init && git submodule update` after clone.

## Font

[Soniano Sans Unicode Font](https://www.1001fonts.com/soniano-sans-unicode-font.html).
